<div class="card border-dark text-light mb-3" style="max-width: 180rem;">
      <div class="card-header bg-dark">
        <h2 class="mb-0">Edit Friend</h2>
      </div>
      
      <div class="card-body">
        <div class="card-text text-dark">  
          <%= render "form", friend: @friend %>
          <br>
          
          <!--<%= link_to "Show this friend", @friend, class:"btn btn-secondary" %> 
          <%= button_to "Delete Friend", @friend, method: :delete, data: {confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm", onclick: "return confirm('Are you sure?');" %>
          <%= link_to "Back to friends", friends_path, class:"btn btn-secondary" %>-->

          <div class="btn-group" role="group" style="margin-right: 10px;">
          <%= link_to "Show this friend", @friend, class: "btn btn-secondary" %>
          </div>

          <div class="btn-group" role="group" style="margin-right: 10px;">
          <%= button_to "Delete Friend", @friend, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger", onclick: "return confirm('Are you sure?');" %>
          </div>

          <div class="btn-group" role="group">
          <%= link_to "Back to friends", friends_path, class: "btn btn-secondary" %>
          </div>

          </div>
          
